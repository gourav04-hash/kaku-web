import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Production optimizations if needed in the future
  // For now, keeping it clean as per Next.js 15+ standards
};

export default nextConfig;
